---
import { Image } from "astro:assets";

type Props = {
	src: any;
	alt: string;
	[key: string]: any;
};

const { src, alt, ...props } = Astro.props;
---

<Image src={src} alt={alt} {...props} transition:name={alt} transition:animate="initial" />
